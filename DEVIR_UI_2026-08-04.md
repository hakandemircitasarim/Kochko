# DEVİR NOTU — KOCHKO arayüz yenileme

Bu dosya bir sonraki ajan içindir. Amaç: **uygulamanın arayüzünü 2026'ya yakışır hâle
getirmeyi tamamlamak.** Aşağıdakiler tahmin değil, cihazda ölçülmüş bilgidir.

Son güncelleme: 2026-08-04, **üçüncü oturum** sonu.

---

## 0-A. GÖRSEL DİL — ÖNCE BUNU OKU (2026-08-07)

> **Bu bölüm diğer her şeyden önce gelir. Burada yazan referansı bilmeden tasarıma
> dokunma — ben bilmeden dokundum ve dört tur boşa gitti.**

### Hedef: Yazio / Duolingo dili

Kullanıcının kendi verdiği referans. Somut kuralları:

| kural | ne demek |
|---|---|
| **Büyük yuvarlak formlar** | kartlar "kutu" değil "yastık". `RADIUS` ölçeği bunun için bir basamak yukarı çekildi (14/20/26/32). |
| **Kalın, basılabilir butonlar** | dolu butonlarda 4px **alt raf** (rengin koyusu). Gölge değil, çerçeve değil — fiziksel gövde hissi. Bu dilin imzası. |
| **Canlı renk** | bölüm başına dolgulu renkli kartlar. Nötr/ölçülü palet bu dile ters. |
| **Az sayı, çok teşvik** | "0/155g" değil "protein iyi gidiyor". Sayı ikinci planda. |
| **Karakter** | maskot/illüstrasyon. **Şu an HİÇ YOK — en büyük eksik bu.** |

### Dört tur neden boşa gitti (tekrarlama)

Kullanıcı "yeşil", "kutu kutu", "iç karartıcı", "boş ve saçma" dedi; ben her seferinde
**son şikâyetin karşı ucuna savruldum**: yoğunsa boşalt → "aşırı boş" oldu; koyuysa aç;
yeşilse mor yap. Çünkü elimde **hedefin görüntüsü yoktu**, sadece nelerden hoşlanmadığı
vardı. "Neden anlaşamıyoruz" sorusunun cevabı buydu.

> **Kural: tasarım yönü belirsizse ÖNCE REFERANS iste** ("beğendiğin bir uygulamanın
> ekran görüntüsü / adı"). Seçenek menüsü sunmak işe yaramıyor — menüdeki seçenekler
> benim hayal gücümle sınırlı. Bir referans, on turluk tahmini tek hamlede bitiriyor.

### Şu ana kadar yapılanlar (hepsi cihazda doğrulandı)

- **Yeşil tamamen kaldırıldı.** Marka: menekşe `#8B5CF6` (koyu) / `#6D3FE0` (açık).
  "Yolunda" durumu gök mavisi. `METRIC_COLORS.calories` da menekşe. Depoda yeşil hex yok.
  *Neden yeşil gitti:* `primary` ile `success` **birebir aynı hex**ti (`#1D9E75`) — yani
  markanın rengi yoktu, başarı rengi ikisini birden yapıyordu.
- **Açık tema varsayılan** (`88fc398`). Eski gerekçe ("auth ekranları sabit koyu")
  07-31'de geçersiz olmuş. Koyu tema Ayarlar > Tema'da duruyor, seçim kalıcı.
- **Yastık formlar + gövdeli butonlar + dolu kahraman kartı** (`ab58931`).
- Metrik paleti 6 hue'dan 3'e indirildi (mavi/kehribar/gül).

### Sıradaki iş (kullanıcının son sözü: "daha estetik olmalı")

**ÜÇÜ DE KAPANDI (2026-08-07, `d70dbbb`, cihazda doğrulandı):**

1. ✅ **Maskot** — `src/components/mascot/KochkoMascot.tsx`: ter bantlı menekşe Koçko
   (SVG; happy/cheer/sleepy; idle bob + göz kırpma; kimlik paleti temadan bağımsız,
   bilerek). Üç yüzeyde: hero konuşma balonu (`CoachBubble`, koç notu karakterin
   ağzından; saat-bazlı fallback yalnız bildirim yuvası ÇÖZÜLDÜKTEN sonra ve yuva
   boşken — yoksa balon belirip sökülüyor, sayfa sıçrıyordu), CoachingNudge mini
   avatarı, kutlama modalında cheer pozu.
2. ✅ **Bölüm başına canlı kart** — StatStrip 2x2 dolgulu yastık: su mavi, adım
   kehribar (`METRIC_COLORS.steps` mor→kehribar, tek tüketici StatStrip), uyku mor,
   kilo gül. Plaka/çip ön planı MÜREKKEP (#2A2140): beyaz kehribarda 1,8:1 ve gülde
   2,97:1 ile WCAG altındaydı; mürekkep dördünde de ≥3,7:1.
3. ✅ **Teşvik kopyası** — kart başına kısa durum cümlesi ("Yarıyı geçtin!"), a11y
   etiketi de içeriyor. Sayı duruyor, ikinci planda.

Animasyon dili: `Reveal` (mount-only kademeli fade+rise — sekme monte kaldığından
pratikte yalnız açılışta), balon spring pop-in (yalnız GERÇEK metin değişiminde,
prev-ref kıyası), su değer-pop'u. Hepsi `useReduceMotion`'a saygılı; maskot döngüleri
`useIsFocused` ile odak dışında duruyor.

**Natif accent:** AppCompat `colorAccent` tanımsızdı → metin seçim vurgusu TURKUAZ
sızıyordu (tartı girişinde ölçüldü). `plugins/withBrandAccent.js` (prebuild kalıcı,
`expo config --type introspect` ile kanıtlı) + yerel `android/` elle eşitlendi + APK
yeniden derlendi; cihazda menekşe doğrulandı. Bildirim tinti de `#1D9E75`→`#6D3FE0`
(depodaki SON yeşil hex'ti; tint cihazda doğrulanamadı — gerçek bildirim gerekir).

Adversarial review turu: 27 ajan, 12 doğrulanmış bulgu, hepsi kapatıldı (tek koç
sesi kapısı, MacroBar izinin açık temada görünmezliği — evet, §0-A tuzağının TA
KENDİSİ bu dosyada da varmış — MAX_FONT_SCALE eksikleri, RADIUS merdiveni tekleşmesi).

**İkinci tur (aynı gün): Koçko yüzeylere yayıldı** — login/register kelime-mark
üstü (84), onboarding ilk slaytı (112, cheer), sohbet boş-durum başlığı (76),
TypingIndicator mini avatarı (24, statik), paylaşılan `EmptyState`'e opsiyonel
`mascot` prop'u (plan arşivi boş durumu kullanıyor). Cihazda doğrulanan: login,
register, onboarding slayt-0, "Kochko yazıyor" göstergesi, plan arşivi Antrenman
boş durumu. Doğrulanamayan: sohbetin sıfır-mesaj boş durumu (test hesabında
geçmiş var — aynı bileşen, mekanik değişim).

### ⚠️ İki kez düştüğüm tuzak — üçüncüsünü yapma

**İz/track rengi yüzeyden VE temadan bağımsız olmalı.** İki kez aynı hata:
kahraman kartı `cardElevated`'e taşıdım → iz `surfaceLight` ile aynı hex'e düştü,
halka kayboldu. Sonra kart açık lavantaya taşındı → beyaz-alfa iz yine kayboldu.
Bir yüzeyin üstüne çizilen her şey (iz, ayraç, çubuk) o yüzeyin rengiyle
**karşılaştırılarak** seçilmeli.

---

## 0a. NEREDEYİZ — 2026-08-06 itibarıyla ölçülmüş durum

Tek bir yüzde yanıltıcı olur, çünkü "bitti" üç ayrı şey demek. Aşağıdakiler tahmin
değil; her biri bu tarihte kontrol edildi.

| Ne | Bitti | Kalan | Dayanak |
|---|---|---|---|
| **Arayüz yenilemesi** | ~%98 | süre/easing birleşmesi (bilerek) | TYPE/SPACE/DEPTH ✔, MOTION ◑ — §5, §12 |
| **Ücretsiz lansmana çıkmak** | ~%95 | Play mağaza görselleri + Play Console adımları | `npm run ci` yeşil (134 test); AAB imzalı, yasal sayfalar canlı |
| **"Vaat edilen her şey gerçekten çalışıyor"** | ~%85 | aşağıdaki kuyruk | `BITMEMISLIK_2026-07-31.md` + 08-06 örneklem denetimi (9 maddenin 3'ü bayat çıktı) |

Üçüncü satırın kuyruğu (31 Temmuz envanteri, **bugün örneklem doğrulaması yapıldı**):

- **A — 13/19 proaktif mesaj ailesi hâlâ donmuş literal.** Kanıt çoğu zaman aynı blokta
  hesaplanıp atılıyor. `ai-proactive` içinde duruyorlar → **edge tarafı, deploy kullanıcıda.**
- **B — ~8 servis yazılmış ama bağlanmamış.** Doğrulandı: `conflict-resolver` **0 import**.
- **C — ~6 şema alanı istemcide okunmuyor.** ⚠️ Envanterin `clinical_rules`/`food_reference`
  maddesi **bayat**: ikisi de `supabase/functions/shared/` içinde okunuyor. C bu kadar
  küçüldü.
- **E — 9 doğrulanmış kusur.** Doğrulandı: `recovery_score` yalnızca `types/database.ts`'te
  var, hiçbir yüzey okumuyor (yazılıyor ama gösterilmiyor).
- **D — 4 madde bilinçli ürün kararı** (IAP, FCM push, Health Connect, sosyal giriş).
  Bunlar "kalan iş" sayılmadı: bunlar iş değil, **karar**.

> **Yüzdenin belirsizliği:** sayı madde sayımından geliyor, ölçülmüş efordan değil.
> Maddeler boyut olarak çok farklı (bir nudge ailesi saatler, Health Connect günler).
> Kabaca 3–4 haftalık iş. D'ye girilirse çok daha fazla.

### ⚠️ Envanterin kendisi kısmen bayat — örneklem denetimi (08-06)

`BITMEMISLIK_2026-07-31.md`'nin 9 maddesi tek tek kontrol edildi; **üçü artık doğru değil.**
Yani kuyruk göründüğünden kısa. Bir sonraki tur envanteri olduğu gibi doğru kabul etmesin.

| Madde | Envanterde | Gerçekte (08-06) |
|---|---|---|
| `clinical_rules` / `food_reference` "hiç okunmuyor" | C | **Bayat** — ikisi de `supabase/functions/shared/` içinde okunuyor |
| "Haftalık menüde alerjen sessizce siliniyor" | E | **Bayat** — `ai-plan` çıkarılan önerileri alerjen adıyla `focus_message`'ta söylüyor (F3/C7) |
| "İlk 3 gün plan hafifletildi vaadi uygulanmıyor" | E | **Bayat** — `ai-plan` Spec 10.5'i uyguluyor. İstemcideki `lightenPlanForReturn` çağrılmayan bir **kopya** → bu bir E kusuru değil, **B kalemi** |
| "Sohbet Geçmişi konu-etiketi filtresi dolmuyor" | E | **Doğru ama kullanıcıya yansımıyor**: filtre satırı `allTags.length > 0` ile kapılı, ekranda hiçbir şey çizilmiyor. Ölü *kod*, ölü *vaat* değil. Mekanizma: etiket yalnızca plan oturumlarına yazılıyor, bu ekran da plan oturumlarını sorgudan bilerek çıkarıyor |
| `recovery_score` okunmuyor | E | **Kapatıldı** (`6a931e0`) |
| "Meal Prep tek yönlü kapı; plan kaydedilmiyor" | E | **Kapatıldı** (`28b8a20`) — üstelik altından **gizli bir tip uyuşmazlığı** çıktı, aşağıya bak |
| `conflict-resolver` bağlanmamış | B | **Doğru** — 0 import |
| `ai-extractor` ilk 100 profil | E | **Kapatıldı** (`19e52ab`) — kök sebep `.limit(100)` değil, `.order()` **yokluğuydu**; rotasyon `shared/fleet-rotation.ts` + 7 test. ⚠️ **Canlıda doğrulanmadı — deploy sende.** |
| Kriz turları güvenlik defterine yazılmıyor | E | **Doğru** — `ai-chat`'te kriz→ledger bağı yok |
| `eas.json` şablon değerlerde | E | **Muhtemelen bayat** — dosyada placeholder bulunamadı |

> **Önemli çekince:** alerjen ve geri-dönüş düzeltmeleri **depoda** var; `ai-plan`'ın
> CANLI sürümünün bunları taşıyıp taşımadığı doğrulanmadı (31 Temmuz deploy tablosunda
> yalnız `ai-chat` ve `ai-proactive` var). Üretime dokunmak bu ajanın işi değil.

### 08-06 kapanış: regresyon süpürmesi + alınan iki "yapmama" kararı

Bu oturumda ~20 commit'te 70'ten fazla dosyaya dokunuldu (266 hedeflik dokunma kodmodu,
tema token'ı, 5 bileşen `Pressable`'a, birkaç veri yolu). Kapanışta dört sekme de
sürüldü: **ana sayfa, sohbet, raporlar, profil — regresyon yok.** Tur içinde ayrıca log,
plan (diyet/antrenman), ayarlar (hedefler, tema, meal-prep, sağlık geçmişi, koç hafızası)
ve rapor alt ekranları doğrulandı.

**Yapılmayan iki iş ve gerekçeleri** (bir sonraki tur bunları yeniden keşfetmesin):

1. **Kalıcı kriz bayrağı — bilerek eklenmedi.** `user_safety_state`'e okuyucusu olmayan
   bir kolon eklemek `seam-check`'in tam olarak savaştığı `inert-wiring` sınıfını yeniden
   üretirdi; okuyucu eklemek ise koç tonu / açık gıda gating'i demek, yani **klinik** bir
   karar. Ayrıca envanterin "hiç kayıt yok" ifadesi de tam doğru değil: kriz turu
   `chat_messages`'a `task_mode='safety'` ile yazılıyor. Ürün sahibinin açık kararını
   hak ediyor.
2. **`profiles.theme_mode` hesap senkronu — ertelendi.** Kolon migration 006'dan beri var,
   `Profile` tipinde bile yok. Ama etkisi yalnızca **ikinci cihazda / temiz kurulumda**
   gözlenebilir, yani bu kurulumda doğrulanamaz. Asıl kullanıcı-görünür kusur (yerelde
   kalıcılık) `a8bb18f` ile zaten kapandı. Yapılacaksa: yerel değer öncelikli kalsın
   (açılışta yanlış tema parlamasın), profil değeri yalnızca yerel değer YOKken benimsensin.

### Tamamlanmış bir göçün kilidi geride kalırsa, kullanıcının seçimini yer (08-06, `a8bb18f`)

`app/_layout.tsx` kayıtlı temayı geri yüklerken `if (saved === 'dark')` diyordu; yazan
taraf ise **her** modu kaydediyordu. Yani "Her Zaman Açık" seçilince tema anında açılıyor,
uygulama her yeniden başlayışında **sessizce koyuya dönüyordu.**

Kilit bilinçliydi ve gerekçesi koddaydı (#R3-6: Ayarlar/Raporlar statik koyu `COLORS`'tan
taşınana kadar). O göç **2026-07-31'de tamamlandı** ama kilit kaldırılmadı. 08-06'da
ölçüldü: statik `COLORS` import eden dosya **0**.

> **Bunu ben de yaşadım ve fark etmedim:** derinlik denemesi sırasında açık temaya geçmiş,
> sonraki yeniden başlatmalarda koyu görüp "tema tercihi sıfırlanmış" diye geçmiştim.
> **Ders: "herhalde sıfırlandı" dediğin an bir kusuru es geçiyor olabilirsin.** Bir
> göçü bitirdiğinde onu bekleyen kilidi de kaldır; yoksa kilit sessizce çalışmaya devam eder.

### Edge kodu yazılabilir, deploy edilemez — nasıl doğrulanır (08-06)

`ai-extractor` düzeltmesi cihazda görülemez (sunucuda çalışıyor, deploy kullanıcıda).
Bu, "doğrulamadan bırak" demek değil: **seçim mantığı saf bir fonksiyona çıkarıldı ve
teste bağlandı** (`shared/fleet-rotation.ts` + 7 test, `npm run test:edge` içinde).
101'inci kullanıcının partiye girdiği artık testle kanıtlı.

> Yazdığım testlerden **biri yanlıştı** ve kod değil test kırıldı: limit 100 iken
> "partideki herkes aç kullanıcı olmalı" diye iddia etmiştim, oysa 50 aç + 50 eski
> olması zorunlu. Test bunu yakaladı, iddia gerçeğe çekildi. Edge tarafında testler
> cihaz ekranının yerini tutan tek şey — iddiayı da kontrol et.

### Ölü kapı, altındaki kusuru saklar (08-06, `28b8a20`)

Meal Prep'in tek yönlü kapısını açınca (gün değiştirilebilir hâle gelince) **gizli bir tip
uyuşmazlığı** ortaya çıktı: `meal_prep_days` şemada `TEXT[]`
(`001_profiles_and_goals.sql:37`) ve `database.ts` de `string[]` diyor — ama servis
`number[]` yazıp `number[]` okuyordu. Yazılan `3` geri `'3'` geliyor, ekrandaki
`prepDay === d.value` (`'3' === 3`) tutmuyor ve **hiçbir gün çipi seçili görünmüyordu.**

Kusur yıllarca görünmezdi çünkü **gün zaten değiştirilemiyordu** — değer hep varsayılanda
kalıyordu. Ders: *bir özelliği kullanılabilir yapmak, altındaki kusuru da görünür yapar.*
Ölü bir kapıyı açarken arkasındaki yolun gerçekten çalıştığını ayrıca doğrula.

Aynı aileden başkası var mı diye tarandı: `as number[]` / `as string[]` cast'lerinden
geriye kalan tek örnek `privacy.service`'in `features_introduced`'ı ve o **doğru**
(`TEXT[]` ↔ `string[]`). Yani aile değil, tek örnekmiş.

---

## 0. EN ÖNEMLİ KURAL

**Emülatörde ekran görüntüsüyle görmediğin hiçbir şeye "çalışıyor" deme.**

`tsc --noEmit` temiz olması *hiçbir şey* kanıtlamaz. Commit başlığına dürüstçe yaz:

- doğruladıysan: `— cihazda doğrulandı`
- doğrulamadıysan: `— CİHAZDA DOĞRULANMADI` + neden doğrulayamadığın

İkinci oturumda `tsc` temizken cihazda çıkan kusurlar: kamera başlığında gölge eksikliği,
`log.tsx`'te dört ekranda birden İptal(14)/Kaydet(13) uyumsuzluğu, koç-hafızasında Türkçe
büyük harf hatası ("PROFIL BILGILERI"), karşılaştırma ekranının iki özdeş sütunu. **Hiçbiri
tip denetiminde görünmez.**

---

## 1. ÜRETİME DOKUNMA

Bunlar **kullanıcıda**, sende değil:

- `supabase functions deploy` (edge function)
- Supabase secret değiştirme
- migration uygulama

Dal üzerinde çalış, commit'le, push'la — üretimi kullanıcıya bırak.

**Güvenlik:** Supabase erişim token'ı sohbet geçmişinde düz metin. Kullanıcıya iş bitince
iptal edip yenilemesini hatırlat.

---

## 2. ORTAM — ÇALIŞAN KURULUM

```
Emülatör : Pixel_3_API_35 (Medium_Phone_API_35 sorun çıkardı, bunu kullan)
Paket    : com.kochko.app  (debug APK kurulu)
Metro    : localhost:8081
```

### Debug APK derleme (gerekirse)
```bash
cd android && ./gradlew assembleDebug -PreactNativeArchitectures=x86_64
```
`-PreactNativeArchitectures=x86_64` **şart**: dört mimarili APK 231 MB ve emülatöre
sığmıyor. Tek mimariyle 85 MB, ~33 saniyede derleniyor.

### Derin bağlantı — gezinmenin en güvenilir yolu
Dokunma koordinatlarıyla uğraşmak yerine doğrudan rotaya git:
```bash
export MSYS_NO_PATHCONV=1
ADB="$LOCALAPPDATA/Android/Sdk/platform-tools/adb.exe"
"$ADB" shell am start -a android.intent.action.VIEW -d "kochko://settings/goals" com.kochko.app
```
Şema `kochko`. `kochko://log`, `kochko://plan/diet`, `kochko://plan/workout`,
`kochko://settings/<sayfa>` çalışıyor.

**Uyarı:** derin bağlantı, o rota zaten monteliyse **iç state'i sıfırlamaz**
(`log.tsx` barkod ekranındayken `kochko://log` seni barkodda bırakır). Önce X'e bas.

**Giriş ekranları oturum kapatmadan açılıyor:** `kochko://login`, `kochko://register`,
`kochko://reset-password`, `kochko://onboarding` oturum açıkken de çiziliyor. Çıkış
yapıp oturumu yeniden enjekte etmene gerek yok.

**Geliştirici "Tools" düğmesi başlığın sağ üstünü kapatıyor** — `Çık`/`İptal` gibi
header düğmelerine dokunamıyorsun, dokunuşu dev menüsü yutuyor. Çözüm: düğmeyi
sürükleyip aşağı taşı (`input swipe 1010 124 300 1700 900`) ya da dev menüsündeki
"Tools button" anahtarını kapat.

### Dokunma koordinatı — tahmin etme, oku
Kaydırma momentumu yüzünden ekran görüntüsünden ölçülen koordinatlar sık ıskalıyor.
Düğümün gerçek sınırlarını al:
```bash
"$ADB" shell rm -f /sdcard/u.xml
"$ADB" shell uiautomator dump /sdcard/u.xml
"$ADB" exec-out cat /sdcard/u.xml | tr '<' '\n' | grep -o 'content-desc="1 bardak su ekle"[^>]*bounds="[^"]*"'
```
`rm -f` **şart** — dump başarısız olursa `cat` eski dosyayı okur ve yanlış teşhis koyarsın
(bir kez modalın açık olduğunu sandım, ekran hâlâ önceki sayfadaydı).

**⚠ 08-07'den beri ana sayfada dump `ERROR: could not get idle state` verebilir:**
Koçko'nun idle animasyonu (nefes döngüsü) uiautomator'ın "UI dursun" beklemesini
doyurmuyor. Maskotlu ekranlarda koordinatı ekran görüntüsünden ölç ya da geçici
olarak OS "hareketi azalt"ı aç (maskot durur, dump çalışır).

---

## 3. OTURUM (parola yazmadan giriş)

Test hesapları:
- `kochko.uitest.0803@example.com` — profili, verisi, onaylanmış diyet planı, **diyet
  taslağı** ve **antrenman taslağı** var (ikinci oturumda üretildi)
- `kochko.uitest.chip@example.com` — temiz thread

Oturumun cihazdaki durumunu **ölç**, tahmin etme:
```bash
"$ADB" shell am force-stop com.kochko.app
"$ADB" exec-out "run-as com.kochko.app cat databases/RKStorage" > RK   # 'adb shell cat' DEĞİL
python sess_inspect.py RK .env    # expires_at / refresh_token yazdırır (token'ları basmaz)
```
Anahtar: `sb-ugoynltxwrkqjwrdxmzt-auth-token`.

**Yeni oturum gerekiyorsa** service_role JWT'yi Supabase Management API'den al:
`GET /v1/projects/<ref>/api-keys?reveal=true` → `name == 'service_role'`.
(GoTrue admin ucu **JWT** ister; `sb_secret_` formatı 401 verir.)

`MSYS_NO_PATHCONV=1` yoksa Git Bash `/sdcard/x.png`'yi Windows yoluna çevirir.

---

## 4. GİRİŞ EKRANINA DÜŞTÜ — ÖNCE ÖLÇ

İkinci oturumda bu **iki kez** oldu ve **her ikisinde de oturum geçerliydi** (biri hatta
kendini tazelemişti, `expires_at` ilerlemişti). Sebep oturum değil, Fast Refresh'in tam
yeniden yükleme yapması ve uygulama durumunun takılmasıydı.

| # | Sebep | Nasıl ayırt edilir |
|---|---|---|
| 1 | Enjekte oturum doldu | Cihazdan oku, `expires_at` bak. |
| 2 | **Tam yeniden yükleme, hidrasyon yetişmedi** (en sık) | ~20 sn bekle; düzelmezse `force-stop` + yeniden başlat, ~10 sn'de gelir. |
| 3 | Dev-launcher yüklenmemiş | `uiautomator dump` → "DEVELOPMENT SERVERS" görüyorsan sunucuya dokun. |
| 4 | Metro soğuk derleme | `node` süreci CPU yakıyorsa çalışıyordur. |

**Fast Refresh ne zaman tam yeniden yükleme yapar:** dosyaya yeni bir üst-seviye bileşen
veya yeni bir `import` eklediğinde. Sadece stil/JSX değiştirdiğinde state korunur — bu
çok işe yarıyor: **modal açıkken düzenle, yerinde güncellensin** (karşılaştırma modalını
ve CTA hizalamasını böyle doğruladım, tekrar LLM çağrısı harcamadan).

### BEMBEYAZ ekran + logcat'te `isMetroRunning()` sonuçsuz (08-07'de yaşandı)

"Loading" bile yazmayan DÜMDÜZ BEYAZ ekran + `ps -T`'de hiç `mqt_js` thread'i yoksa
dev launcher Metro sorbesinde asılı demektir. 08-07'de kök sebep: **adb reverse
tüneli sessizce öldü** (listede duruyor, cihazdan `nc localhost 8081` bağlanıp HİÇ
veri vermiyor) ve `isMetroRunning()` bu yarı-açık sokette süresiz bekliyor. `adb
kill-server` bile tüneli düzeltmedi; NAT (10.0.2.2) çalışıyordu ama launcher yine
takıldı — çünkü asıl Metro'nun WS katmanı da yorulmuştu. **Çözüm: Metro'yu öldürüp
yeniden başlat** (`netstat -ano | findstr :8081` → taskkill → `npx expo start`).
Teşhis komutu: cihazdan `nc -w 3 localhost 8081` — boş dönüş = ölü tünel;
"Connection refused" = tünel yok (sağlıklı hızlı-hata). İki saatlik kör tahmin
yerine önce bunu ölç.

### "Loading from 10.0.2.2:8081…" ekranında TAKILDIYSA (08-06'da yaşandı)

Sunucu ana makinede 200 dönüyor ama cihaz paketi çekemiyor ve dakikalarca öyle kalıyor.
Sebep emülatörün 10.0.2.2 NAT'ının bozulması (bende ağı `svc wifi disable/enable` ile
kapatıp açtıktan sonra oldu). Yeniden başlatmak çözmüyor. Çözüm NAT'ı baypas etmek:

```bash
"$ADB" reverse tcp:8081 tcp:8081
"$ADB" shell am force-stop com.kochko.app
"$ADB" shell am start -a android.intent.action.VIEW \
  -d "exp+kochko://expo-development-client/?url=http%3A%2F%2Flocalhost%3A8081" com.kochko.app
```

45 saniyede açıldı. **Emülatörün ağını kapatma** — çevrimdışı afişini görmek cazip ama
bedeli bu.

**Metro'nun paket ürettiğini kanıtlama komutu:**
```bash
curl -s -o /dev/null -w "%{http_code}" --max-time 560 \
  "http://localhost:8081/node_modules/expo-router/entry.bundle?platform=android&dev=true&transform.routerRoot=app"
```
`200` bekle. `/index.bundle` **yanlış yoldur** (Expo Router'da 404).

---

## 5. TASARIM SİSTEMİ — `src/lib/design.ts`

Tek kaynak. Ham `fontSize` yazma.

```
TYPE.display   40/44  — ekran başına BİR hero figürü (kalori halkası, TARTI/ADIM girişi)
TYPE.title1    30/36  — onboarding ekran başlıkları
TYPE.title2    24/30  — sayfa başlıkları
TYPE.title3    20/26  — kart figürleri, bölüm başlıkları, boş-durum başlıkları
TYPE.headline  17/23  — kart başlıkları, liste satırı başlıkları
TYPE.body      15/22  — OKUMA adımı: sohbet, ayar satırları, input içi metin, buton etiketi
TYPE.bodyStrong 15/22 — vurgulu gövde, buton etiketi
TYPE.callout   14/20  — ikincil kopya, ALAN ETİKETLERİ (paylaşılan Input bunu kullanır)
TYPE.caption   13/18  — üst veri, açıklama, hata mesajı, çip/rozet etiketi
TYPE.overline  11/14  — BÜYÜK HARF bölüm etiketi (+0.8 tracking)
TYPE.footnote  11/15  — en küçük okunur adım
```

Ayrıca: `SPACE`, `GUTTER` (20), `RADII`, `elevation()`, `MOTION`, `surface()`, `alpha()`.

**İkinci oturumda pekişen kurallar:**

- **11px'i "okuyup eyleme geçilecek" hiçbir şeye verme.** Ölçek bunu açıkça söylüyor.
  "Bunu yedim"/"Bunu yaptım" çipleri, egzersiz form notları, giriş satırı açıklamaları —
  hepsi 11'deydi, hepsi 13'e çıktı.
- **Alan etiketi = `TYPE.callout`/w500.** Paylaşılan `Input` bunu kullanıyor; sayfa içinde
  elle etiket yazarsan aynı formda iki farklı spec olur (goals.tsx'te tam bu vardı).
- **Aynı yuvayı paylaşan durumlar aynı boyutta olmalı.** Tartı ekranında hata/birim/delta
  satırı tek yuvayı paylaşıyordu ama 11/11/13'tü.
- **Yan yana duran iki buton aynı spec'te olmalı.** Dört alt ekranda İptal 14, Kaydet 13'tü.
- **`FONT.lg + 2` gibi aritmetik yazma.** Ölçeğin ölçek olmaktan çıktığı yer orası.
- **Kamera üstündeki metin gölge ister.** `log.tsx`'te `ON_CAMERA_TEXT` var, kullan.
- **Başlıklar cümle düzeni.** ("Barkod Tara" → "Barkod tara", "Tartı Kaydı" → "Tartı kaydı".)
  **İstisna, sandığından geniş:** ayarlar ve raporlar yüzeylerinde yalnızca sayfa başlığı
  değil, **kart başlıkları da** Title Case ve kendi içinde tutarlı — "Mevcut Hedef",
  "Hedef Türü", "Kilo Trendi", "Güç Özeti", "Haftanın Günleri"… Oraya dokunma.
  Bunu 08-06'da kendi hatamla öğrendim: `TempoChart`'ın başlığını "Kilo Tempo" →
  "Kilo tempo" yapmıştım; TempoChart **yalnızca `settings/goals`'ta** render oluyor ve
  o ekranda tek cümle-düzeni başlık o oldu. `PhaseTimeline`'ın "Hedef Fazları"nda da aynı
  hatayı yapmıştım (o da ayarlar + raporlar sekmesinde). İkisi de geri alındı.
  **Bir başlığın düzenine karar vermeden önce bileşenin NEREDE render olduğuna bak** —
  `grep -rn "<BilesenAdi"`.
- **`textTransform:'uppercase'` Türkçe'de BOZUKTUR** (i→I). `toLocaleUpperCase('tr-TR')`
  kullan. Uygulama geneli süpürüldü (7 yer); ekranda **"DIĞER", "ALERJI", "AMELIYAT",
  "PORSIYON", "E-POSTA DEĞIŞTIR"** yazıyordu. Şu an canlı hiçbir yerde kalmadı —
  yenisini eklersen aynı hatayı geri getirirsin.
- **Yayılımdan SONRA elle `lineHeight` yazma.** `{ ...TYPE.body, lineHeight: 20 }` token'ın
  22'sini ezer; token'a bağladığını sanarsın ama satır aralığı eski sıkışık hâlinde kalır.
  26 yerde vardı, temizlendi. Tarama: `grep -rn "\.\.\.TYPE\.[a-zA-Z]*,[^}]*lineHeight"`
- **Paylaşılan bileşeni büyütmek uzak ekranlarda kırpar.** `Input` 13→15'e çıkarıldığında
  `chat-history`'nin yarım genişlikteki tarih alanı ikinci satıra sarıp kırpıldı — ve bu
  iki oturum boyunca kimse o ekranı açmadığı için görülmedi. Paylaşılan bir metin boyutunu
  değiştirdiğinde, onu **dar kaplarda** kullanan yerleri ayrıca tara:
  uzun `placeholder` + `flex: 1` + `flexDirection: 'row'`.

---

## 6. BİTMİŞ VE CİHAZDA DOĞRULANMIŞ

| alan | ne yapıldı |
|---|---|
| **Ana Sayfa** | hero halkası, StatStrip, GoalProgressCard, PlanOverviewCards, ActivityTimeline |
| **Kochko (sohbet)** | gövde metni + yazma alanı, RichMessage kartları |
| **Raporlar / Profil / Onboarding / Kayıt** | başlıklar, boş durumlar, alan etiketleri |
| **Plan akışı** | boş durum, MealCard, PlanDayAccordion, PlanActiveView, **ExerciseCard**, **AlternativeComparisonModal** |
| **`app/log.tsx`** | **55 ham fontSize → 0.** Ana ekran, izin kapısı, kamera kaplaması, tartı, uyku, adım, toparlanma, ses, başarı ekranı |
| **Ayarlar — 34 sayfanın TAMAMI** | **~380 ham fontSize → 31.** Kalan 31'in 28'i `premium.tsx` (ölü yüzey), 3'ü kasıtlı bırakılan işaret glifi (★ ✓ +) |
| **Sohbet** | `ChatThreadScreen` 56→0, `RichMessage` 25→0. Mesaj gövdesinin iki farklı boyutta render edilmesi düzeltildi |
| **Raporlar** | weekly/monthly/calendar/daily — 99→0 |
| **İlerleme + Ana sayfa** | `(tabs)/progress` 37→0, `(tabs)/index` 30→0 |
| **Paylaşılan primitifler** | `Card`, `Button`, `Input`, `EmptyState`, `LoadErrorState`, `SectionHeader` |
| **Türkçe büyük harf** | uygulama geneli süpürüldü, canlı `textTransform:'uppercase'` kalmadı |
| **Plan akışı (3. oturum)** | PlanActiveView, PlanPreviewCard, PlanEmptyState, plan/history, PlanManagerScreen, PlanDayAccordion, PhaseTimeline, MealCard, FullPlanModal, TempoChart, PlanChatComposer |
| **Profil + dashboard (3. oturum)** | `(tabs)/profile`, HeroSection, InsightCard, ProfileCompletionDonut, ActivityTimeline, CoachingNudge, GoalProgressCard, StatStrip, PlanOverviewCards, LifeEventCard |
| **Giriş akışı (3. oturum)** | login, register, onboarding, reset-password (`app/index.tsx`'in iki hata yüzeyi hariç — §6b) |
| **Kuyruk (3. oturum)** | all-time, ErrorBoundary, CircularProgress, ProgressChart, FeedbackButtons, SyncStatusCard, `(tabs)/_layout`, Toast, DateTimeField, ToggleRow, ComplianceScore, StreakBadge, DeviationTag, OfflineBanner, TypingIndicator |

Dal: `claude/KOCHKO`.

### 6b. Cihazda DOĞRULANMAYANLAR (dürüst liste)

Hepsi mekanik token takası, `tsc` + `arch-guards` temiz — ama gözle görülmedi, çünkü
ancak özel bir durumda çiziliyorlar:

| yüzey | neden görülemedi |
|---|---|
| `app/index.tsx` — "Profilin yüklenemedi" | profil çekme hatası gerek; ağı kesmek Metro'yu da kesiyor |
| `app/index.tsx` — "Hesap silme talebi bekliyor" | DB'de `deletion_requested_at` gerek |
| Toast, FeedbackButtons, TypingIndicator | eyleme/LLM turuna bağlı geçici yüzeyler |
| SyncStatusCard | çevrimdışı kuyrukta bekleyen kayıt gerek |
| DeviationTag | en az iki tartı kaydı gerek (08-05'te bir kayıt girildi, hâlâ iki gün yok) |
| ErrorBoundary | zorlanamadı |

**08-05 güncellemesi:** test hesabına gerçek bir tartı kaydı girildi (86 → 85,4 kg), bu
sayede `ProgressChart` ve `all-time`ın "Toplam İlerleme" kartı görülebildi — ve ikisi de
kusurlu çıktı (bkz. §11). Listeden düştüler.

---

## 11. VERİ DÜRÜSTLÜĞÜ CEPHESİ (08-05, açıldı ve beş bulgu kapatıldı)

Ölçek bittikten sonra uygulamayı sürerken çıkan aile: **tek bir ölçümü, ölçülmüş bir
DEĞİŞİM gibi göstermek.** İlk kez tartılan biri için karşılaştırılacak ikinci bir gün
yok; uygulama yine de güvenle "0,0" yazıyordu.

Kapatılanlar (commit `e09f1f9` + `1be5c67`, hepsi cihazda doğrulandı):

| yüzey | ne diyordu | kapı |
|---|---|---|
| `(tabs)/progress` kilo kutucuğu | yeşil "0,0" rozeti | `weights.length >= 2` |
| `reports/all-time` "Toplam İlerleme" | manşette "0,0 kg", altında "85,4 → 85,4" | `weightDays >= 2` (aynı isteğe `count:'exact'`) |
| `reports/monthly` "Kilo Trendi" | üç sütunda aynı sayı | `weightData.length >= 2` |
| `ProgressChart` | "En düşük 85,4 / Son 85,4 / En yüksek 85,4" + tek nokta | `data.length < 2` |
| `(tabs)/progress` özet kutucukları | "28 gün ort." ama değer yalnız kayıtlı günlerin ortalaması | etiket gerçek paydayı yazıyor |
| `reports/weekly` "Haftanın Günleri" | aynı gün hem "En İyi" hem "Zorlu gün" (haftanın tek kayıtlı günü) | `best_day !== worst_day` |

---

## 13. ONDALIK YAZIMI — bir aile daha (08-06, `a6d260f` + `fe362fd`)

Uygulama her yerde **"85,4 kg"** diyor, ama ham sayı basan yüzeyler **"85.4"** yazıyordu.
Bazı yerlerde **aynı satırda iki yazım** vardı:

| yüzey | nokta | virgül |
|---|---|---|
| tartı kaydı | alan `85.4` | hemen altında `+0,1 kg` |
| haftalık rapor kilo trendi | değer `85.4 kg` | yanındaki fark `-0,6` |
| hedef ayarları | başlık `Mevcut: 85.4 kg` | altındaki grafik `86,0 → 78,0` |

`src/lib/units.ts` → **`formatDecimal()`** (units.ts zaten biçimlendirme yardımcılarının
evi) ve **13 çağrı yeri**: 4 giriş beslemesi (log tartı + basamaklayıcı, onboarding kilo
& hedef kilo, edit-profile, goals) + 9 görüntü (goals "Mevcut", coach-memory 2 satır,
goals faz rozeti, multi-phase-goals, calendar seçili gün, weekly kilo trendi, strength
1RM + son ağırlık, PlanActiveView 2 drift mesajı).

Ayrıştırma tarafı **zaten** ikisini de kabul ediyordu (`parseFloat` öncesi `replace`) —
davranış değişmedi, yalnızca yazım düzeldi.

**Dışarıda bırakıldı:** `health-export` ve `coach-sharing` metin raporları (uygulamadan
dışarı paylaşılıyorlar, cihazda doğrulanamadı) ve `debug-mode` (geliştirici yüzeyi).

### Aynı ailenin ikinci yarısı: ham ISO tarihler (`1e39ca7`)

Uygulama tarihleri ISO **saklıyor** (doğru) ama bazı yüzeyler bunu doğrudan basıyordu:

- **Sağlık Geçmişi** listesi `2026-08-05` yazıyordu — oysa o kaydı oluşturan alan bir
  ekran önce "5 Ağustos 2026" diyordu. Aynı değer, aynı akış, iki format.
- **settings/goals** `Tahmini tamamlanma: 2026-10-31` yazıyordu — ana sayfadaki kart
  **aynı veriyi** "30 Eki" diye gösteriyordu.

`src/lib/day-boundary.ts` → **`formatISODateTR(iso, 'short'|'long')`**, 4 çağrı yeri
(health-events, goals, GoalProgressCard — kendi kopyası kaldırıldı, ChatThreadScreen'in
geri-tarih a11y etiketi).

> **Öğleye sabitleme bilinçli:** `new Date('2026-08-05')` UTC gece yarısı olarak
> ayrıştırılıyor ve negatif ofsetli bölgelerde bir gün geri kayıyor. Boş/geçersiz
> değerde `null` dönüyor ki çağıran satırı gizlesin, "Invalid Date" basmasın.

**`toFixed` ve binlik ayracı taraması temiz çıktı** — `toFixed()` kullanıp `replace`
etmeyen tek satır yok, sayı ayraçları `toLocaleString('tr-TR')` ile tutarlı. O iki alt
aile kapalı.

**Bu turda incelenip KUSUR BULUNMAYANLAR** (tekrar kovalanmasın): uyku kaydı ekranı
(`23:00/07:00` gerçek değer değil placeholder; ikisi seçilince "8 saat uyku" önizlemesi
çiziliyor, anormal süreler turuncu, Kaydet doğru şekilde devre dışı) ve onboarding'in
"86" göstermesi (AsyncStorage'daki eski test taslağı, kod değil).

**Bu ailede TEMİZ olanlar** (kontrol edildi, dokunulmadı): `coach-sharing` ve
`health-export` zaten `>= 2` arıyor; `progress`'in uyum/su/uyku/adım trendleri iki
pencerenin ikisinde de veri şartı arıyor; `TempoChart`'ın carry-forward'ı bilinçli ve
zaten kesikli+soluk çiziliyor, ETA de `>= 2` gerçek ölçüm istiyor. Haftalık raporun
**AI metinleri de dürüst** — cihazda "Veri eksikliği nedeniyle belirgin bir sapma konusu
saptanamadı" ve "Bu hafta yalnızca 2 Ağustos için uyum verisi var" yazıyor; yani sunucu
zaten bu dili konuşuyor, istemcinin ondan geri kalması tutarsızlıktı.

**Sunucu tarafında kalan bir örnek (kullanıcıda):** haftalık raporun "Güç Özeti"
kartı, hiç alkol kaydı olmayan ve tek gün kayıt tutmuş bir kullanıcıya "Alkol alımı bu
hafta da 0 kcal kaldı; geçen haftayla aynı çizgiyi korumuşsun" diyor — olmayan bir
seriyi övüyor. Edge tarafı, üretim kullanıcıda, dokunulmadı.

### ✅ Kapandı — `TempoChart` x ekseni (08-06, `db334f7`)

12 haftalık hedefte eksende sadece `0h` yazıyordu. Sebep kütüphanenin kaynağında,
`react-native-chart-kit/dist/AbstractChart.js` → `renderVerticalLabels`:

```js
return labels.map(function (label, i) {
  if (hidePointsAtIndex.includes(i)) { return null; }
```

`hidePointsAtIndex` yalnızca **noktayı** değil aynı indeksin **eksen etiketini de**
siliyor. TempoChart bunu ölçüm olmayan haftaların noktalarını gizlemek için
kullanıyordu — meşru bir amaç — ve tek ölçümlü bir hedefte 13 etiketten 12'si birden
yok oluyordu.

Çözüm: nokta gizleme `getDotColor` üzerinden (ölçülmeyen haftaların dolgusu saydam).
Görsel niyet aynı, etiketler duruyor. Cihazda `0h 2h 4h 6h 8h 10h 12h` okunuyor.

> **Genel ders:** bir grafik kütüphanesinin bir prop'u birden fazla şeyi kontrol
> ediyor olabilir. "Nokta gizle" diye okuduğun şey ekseni de siliyordu.

---

## 7. ÖLÇEK İŞİ BİTTİ — kalan ham `fontSize` yok

**Üçüncü oturumda kapandı.** 491 → 244 → **0**. Canlı hiçbir yüzeyde ham `fontSize`
kalmadı. Bugün bağlananlar: plan akışı (11 dosya), profil + dashboard (10), giriş /
kayıt / onboarding / şifre sıfırlama (5), kuyruk (18).

Kodda hâlâ görünen 44 sayı **kasıtlı**, dokunma:

```
28  app/settings/premium.tsx   ölü yüzey (FREE_LAUNCH altında gizli)
11  src/lib/design.ts          ölçeğin kendi token tanımları
 3  weekly-menu / recipes / household   işaret glifi: +  ★  ✓
 2  yorum satırı (constants.ts, ErrorBoundary.tsx)
```

Tarama: `grep -rn "fontSize:" app src --include=*.tsx | grep -v "TYPE\."`

**Tasarım sisteminin durumu:** TYPE ✔, SPACE ✔ (constants.ts'teki SPACING/RADIUS zaten
aynı merdivene göre yeniden ayarlanmış), **DEPTH ✔** (08-05, kenar üzerinden — §8),
**MOTION ◑** (08-05 dokunma geri bildirimi, 08-06 kart ölçeği — §12; geriye yalnızca
süre/easing birleşmesi kaldı).

`elevation()` / `surface()` / `RADII` / `SPACE` sıfır kullanımda ama bunlar **isim**
meselesi: `SPACING`/`RADIUS` aynı merdiveni taşıyor, `elevation()` ise koyu temada
ölçülerek elenmiş durumda (§8). Yani bu dördü için "kullanılmıyor" bir eksik değil.

---

## 12. MOTION (08-05 — dokunma dili bitti, ikisi duruyor)

### ✅ Bitti: `MOTION.pressOpacity` (`7823b74`)

React Native'in `TouchableOpacity` varsayılanı `activeOpacity = 0.2`
(`node_modules/react-native/.../TouchableOpacity.js`: `this.props.activeOpacity ?? 0.2`).
Uygulamanın 272 dokunma hedefinin **224'ü bunu hiç belirtmiyordu** — yani baskın dokunma
geri bildirimi bir karar değil kazaydı. Cihazda ölçüldü: "Öğün ekle" basılınca düğme ve
etiketi neredeyse yok oluyordu. Belirten 48 tanesi de kendi arasında dağılmıştı
(0.6 / 0.7 / 0.8 / 0.85 / 0.9).

Şimdi: **266 hedef token'da, 6 bilinçli opt-out (`activeOpacity={1}`), 0 varsayılan.**

> **Kodmod notu (yeniden gerekirse):** JSX açılış etiketini regex ile bulma. Prop'lar
> süslü parantez, string, template literal ve `//` yorumu içerebiliyor; bunların içinde
> `>` geçiyor. `app/index.tsx`'teki `await'siz` yorumu ilk denemede ayrıştırıcıyı kırdı.
> Tarayıcı süslü derinliği + tırnak + yorum atlamalı olmalı, ve **iki fazlı** çalışmalı
> (önce hepsini ayrıştır, sonra yaz) — yoksa ortadaki bir hata yarım yazılmış ağaç bırakır.
>
> **Basılı hâli cihazda görmenin yolu:** `adb shell input motionevent DOWN x y` → ekran
> görüntüsü → `UP`. Hareket ekran görüntüsüyle doğrulanamaz sanma, bu ikili çalışıyor.

### ✅ Bitti: `MOTION.pressScale` (08-06, `88af110`)

`src/components/ui/PressableCard.tsx` — `Pressable`'ın style callback'i üzerinden hem
opaklık hem ölçek. **Beş kart köküne** bağlandı: dashboard plan kartı, GoalProgressCard,
ProfileCompletionDonut, LifeEventCard, PlanPreviewCard.

**Ayrım bilinçli:** yalnızca KARTLAR. Büyük bir yüzeyi soldurmak "ekran değişti" gibi
okunuyor, küçültmek "buna bastım" diyor; küçük bir hedefte ise %2 ölçek fark edilmiyor.
Satırlar, çipler ve ikon düğmeleri `TouchableOpacity` + `activeOpacity`'de kalıyor.
Bu kural bileşenin dosya başı yorumunda yazılı — yeni bir kart eklerken oraya bak.

> ⚠️ `Pressable`'da `activeOpacity` HİÇBİR ŞEY YAPMAZ. Bir bileşeni bu sarmalayıcıya
> taşırken `activeOpacity` prop'unu bırakırsan sessizce ölü prop olur; kaldır.

### Duran tek madde

- **Süre/easing birleşmedi.** `LayoutAnimation.Presets.easeInEaseOut` (300 ms) 4 yerde,
  `Animated.timing` süreleri 150/180/220/300/600/700/800 diye dağınık. `MOTION.base` 220.
  Kazanç küçük, risk sıfır değil (presetler elden ayarlı) — bilerek bırakıldı.

> **Basılı hâli cihazda yakalarken:** `motionevent DOWN` + `UP` bir DOKUNUŞ sayılıyor ve
> `onPress`'i ateşliyor. Ekran görüntüsünü **DOWN ile UP arasında** al, yoksa uygulama
> gezinir ve başka ekranı çekersin (bu turda bir kez oldu).

> **Bu ölçümün asıl dersi:** ilk üç denemede de "fark yok" çıktı ve neredeyse
> "elevation Android'de işe yaramıyor" diye not düşüyordum. Kırmızı 4px kenarlıkla
> **aleti sınadım**: o da görünmüyordu. Emülatörün ağını kapatıp açtığımda Metro
> soketi düşmüş, Fast Refresh o andan beri hiçbir şeyi iletmiyormuş. **Ekran
> görüntüsü "değişmedi" diyorsa, önce değişikliğin ekrana ULAŞTIĞINI kanıtla.**
> Görünür bir işaret (kalın kırmızı kenarlık) 30 saniye sürüyor ve yanlış bir
> teşhisi kayda geçmeni engelliyor.

**Verimli döngü (ikinci oturumda ~50 dosya böyle bitti):**
1. Grubun "öncesi" ekran görüntülerini toplu al (derin bağlantı + `screencap`, ~7 sn/sayfa).
2. Kalıp eşlemeli script ile hepsini birden token'a bağla (aşağıdaki harita).
3. `npm run lint` + `npm run arch-guards`.
4. Tek yeniden başlatma, sonra "sonrası" görüntülerini toplu al, 3'lü kontak sayfası hâlinde bak.

Kalıp haritası (en özelden genele; sıra önemli):
```
FONT.xl|lg + w600-800  -> TYPE.title3      FONT.sm + w500-600 -> TYPE.bodyStrong
FONT.md   + w500-800   -> TYPE.headline    FONT.sm            -> TYPE.body
FONT.md                -> TYPE.body        FONT.xs + w700     -> TYPE.caption/w700
FONT.sm   + w700-800   -> TYPE.bodyStrong/w700                -> ALL-CAPS ise TYPE.overline
                                           FONT.xs            -> TYPE.caption
```

**Script yazarken dört tuzak:**
- `...TYPE.x` yayılımı kendinden ÖNCE gelen `fontWeight`'i ezer.
  Tara: `grep -n "fontWeight:[^,]*,[^}]*\.\.\.TYPE" <dosya>`
- Yayılımdan SONRA kalan `lineHeight` token'ı ezer (§5'teki kural).
- 11'in altındaki ham sayılar (9/10) kalıba uymaz — kabın genişliğini **önce oku**, sonra
  karar ver: geniş yerde `caption`(13), dar ızgara/kutucuk/baloncuk içinde `footnote`(11).
- JSX yorumu (`{/* */}`) yalnız JSX çocuk konumunda geçerli; ternary'nin içine koyarsan
  derleme kırılır. İfade bağlamında `//` kullan.

**Bash heredoc uyarısı:** uzun/çok satırlı Python script'lerini `<<'PY'` ile geçirme —
bu oturumda bir kez satır ortasından bölündü. Script'i dosyaya yazıp `python dosya.py`
ile çalıştır.

## 8. KARARA BAĞLANANLAR ve KULLANICIDA KALAN

### ✅ Kapandı — iki profil yüzdesi çelişkisi (08-05, `afcc80b`)

Dashboard halkası "PROFİL" etiketiyle %15, profil sekmesi "Profilin %68 tam" diyordu.
İkisi de doğruydu, farklı şey ölçüyorlardı: halka 13-görevlik onboarding ilerlemesi,
profil sekmesi 24-alanlık doluluk. **Çelişki sayılarda değil isimlerdeydi.** Karar:
her yüzey ölçtüğü şeyi söylesin. Halka artık `GÖREVLER` / "Görevlerini tamamla", ipucu
da CTA ile aynı kaynaktan geliyor ("Sıradaki: Günlük rutinini anlat" — dokununca tam o
görev açılıyor; eskiden ipucu alan-doluluk hesabından gelip başka bir konu açıyordu).
Profil sekmesi değişmedi. Mantık aynı, yalnızca isimler dürüstleşti.

### ✅ Kapandı — derinlik (08-05, `b0f2c75`)

Gölge tabanlı derinlik **bırakıldı**, yerine kart kenarı güçlendirildi:
`border 0.08 → 0.16`, `divider` bilerek 0.08'de.

Gerekçe ölçüldü: Android `shadowColor/Opacity/Offset/Radius`'u tamamen yok sayar,
yalnız `elevation` sayısını çizer; `#0D0D12` üstünde siyah gölge tanım gereği görünmez
(Card'a `elevation(1)` ve `(2)` verildi, cihazda hiçbir fark yok, geri alındı). Kart
dolgusunu bir ton açmak da bedava değil: `cardElevated` ile `surfaceLight` aynı ton, yani
kart **içindeki** satır/çip hiyerarşisi düzleşirdi. Koyu temada bir yüzeyi kaldıran şey
kenarıdır. `divider`'a dokunulmadı ki grup sınırı güçlü, iç ayraçlar sessiz kalsın —
ayar listelerinde cihazda doğrulandı. `design.ts`'in DEPTH bölümü artık bunu yazıyor.

### Kullanıcıda kalan

**Edge, alternatif planı taslak satırına yazıyor ama bunu tutarsız yapıyor.**
Dört çağrının ikisinde taslak sürümü arttı (v1→v2, v2→v3), ikisinde artmadı. İstemci
tarafı artık iki durumda da doğru davranıyor (`preAltSnapshot` donduruluyor, "Plan A"
gerçekten geri yüklüyor — bkz. commit `551773d`), ama **asıl düzeltilmesi gereken yer
edge tarafı** ve üretim kullanıcıda olduğu için dokunulmadı.

---

## 9. YÖNTEM (işe yarayan döngü)

1. Paylaşılan bileşen var mı? Varsa **önce onu** düzelt.
2. Değişikliği yap → `npm run lint` + `npm run arch-guards`
3. Emülatörde **gerçekten aç ve ekran görüntüsü al**
4. Gördüğün şeyin **değiştirdiğin şey olduğundan emin ol**
5. Commit + push, doğrulama durumunu başlığa yaz
6. **Bir kusur görürsen: ölçmeden düzeltme.**

7. **Ekran görüntüsü "hiç değişmedi" diyorsa, önce aleti sına.** Görünür bir işaret koy
   (4px kırmızı kenarlık), o da görünmüyorsa sorun senin değişikliğinde değil,
   Fast Refresh'in kopmasındadır. Üçüncü oturumda derinlik denemesinin üç ölçümü de
   bu yüzden geçersiz çıktı — sınamasaydım "elevation Android'de işe yaramıyor" diye
   yanlış bir teşhisi devir notuna yazacaktım.

6. maddenin bedeli somut: ikinci oturumda su çipinin "bozuk" olduğunu sandım, iki ayrı
hipotez kurdum, ikisi de kodda karşılık bulmadı — temiz açılışta çip ilk dokunuşta
çalıştı. Dokunuşlarım kaydırma momentumu yüzünden ıskalıyormuş. **Hayalet hata
bildirmedim.** Aynı kural gerçek bir hatayı da yakalattı: karşılaştırma ekranının iki
özdeş sütununu üç ayrı üretimde ölçüp, taslak sürümünün v1→v2 gittiğini görüp sebebi
kanıtladım.

---

## 10. ÇALIŞMA BİÇİMİ NOTU

Gözetimsiz çalışma bu kurulumda mümkün değil (`/loop` + `ScheduleWakeup` `session-only`;
bulut zamanlamasında emülatör yok).

İşe yarayan: **tek turda uzun çalışmak.** Turu erken bitirip uyandırma bekleme; art arda
birden çok bileşeni bitir, her birini doğrula, commit'le. Kullanıcı "devam" yazdığında
yine uzun bir tur çalış. Kalite > hız.
